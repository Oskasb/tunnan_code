"use strict";

define([
	'application/EventManager'
],
	function(
		event
		) {
	/*
		var gameScreen;
		var width;
		var height;

		var setupMouseListener = function(element) {
			gameScreen = element;
			gameScreen.style.pointerEvents = 'auto';
			width = gameScreen.offsetWidth;
			height = gameScreen.offsetHeight;
			event.registerListener(event.list().WINDOW_RESIZED, handleResize);
		};

		var handleResize = function() {
			width = gameScreen.offsetWidth;
			height = gameScreen.offsetHeight;
		};

		var getElement = function() {
			return gameScreen;
		};

		var getWidth = function() {
			return width;
		};

		var getHeight = function() {
			return height;
		};

		return {
			setupMouseListener:setupMouseListener,
			getElement:getElement,
			getWidth:getWidth,
			getHeight:getHeight
		}
   */
	});